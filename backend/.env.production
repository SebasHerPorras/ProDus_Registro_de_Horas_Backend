# ============================================
# CONFIGURACIÓN DEL BACKEND - PRODUCCIÓN
# ============================================
# Máquina Virtual: 10.178.2.33
# Este archivo se copia como .env en el servidor de producción

# Django Settings
DEBUG=False
SECRET_KEY=ProdusTI.202&
ALLOWED_HOSTS=10.178.2.33,localhost,127.0.0.1

# Database PostgreSQL
DB_ENGINE=django.db.backends.postgresql
DB_NAME=produs_db
DB_USER=produs_user
DB_PASSWORD=ProdusTI.202&
DB_HOST=localhost
DB_PORT=5432

# CORS - URLs del frontend permitidas (separadas por coma)
# Frontend servido por Nginx en http://10.178.2.33/
CORS_ALLOWED_ORIGINS=http://10.178.2.33

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME_HOURS=8
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# HTTPS / SSL Settings (cambiar a True cuando tengas certificados)
USE_HTTPS=False
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# Gunicorn settings
GUNICORN_WORKERS=4
GUNICORN_BIND=0.0.0.0:8000
